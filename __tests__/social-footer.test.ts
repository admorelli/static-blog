import { describe, it } from 'vitest';

describe('SocialFloatingFooter', () => {
  it.skip('renders three social links with correct hrefs', () => {
    // Disabled during SocialFloatingFooter throttling work.
    // Asserts footer link count, hrefs, aria-labels, and target=_blank.
  });

  it.skip('renders social icons from official CDN', () => {
    // Disabled during SocialFloatingFooter throttling work.
    // Asserts social icon images load from cdn.simpleicons.org.
  });
});
